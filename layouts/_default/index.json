{
    "items" : [
        {{ range where $.Paginator.Pages "Section" "blogs" }}
        {
            "url": "{{ .RelPermalink }}",
            "description": "{{ .Description }}"
        }
        {{ end }}
    ]
}